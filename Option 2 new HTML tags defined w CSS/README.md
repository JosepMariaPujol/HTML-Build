# Option 2 new HTML tags defined w CSS

This option creates new HTML tags for each of the building objects. 
Instances of this objects can then be seperately classed - it doesn't seem to work
very well...